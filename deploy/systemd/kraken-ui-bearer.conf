# Install as /etc/systemd/system/kraken-ui.service.d/bearer.conf.
# Both KrakenWAF and Kraken UI may use the same root-owned credential source.
[Service]
LoadCredential=BEARER_PASSWORD:/etc/krakenwaf/secrets/BEARER_PASSWORD
Environment=BEARER_PASSWORD_FILE=%d/BEARER_PASSWORD
